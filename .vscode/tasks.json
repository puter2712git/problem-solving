{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "cppbuild",
      "label": "cppbuild",
      "command": "/usr/bin/g++",
      "args": [
        "-g",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}.exe",
        "${file}",
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [
        "$gcc"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}